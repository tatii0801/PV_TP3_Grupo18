const Nav = () => {
  return (
    <nav className="contenedor margen">
      <a href="#">Inicio</a>
      <a href="#">Proyectos</a>
      <a href="#">Perfil</a>
      <a href="#">Detalles</a>
    </nav>
  );
};

export default Nav;
